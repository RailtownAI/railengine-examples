These samples use the Railengine Python SDK and [Railtracks](https://github.com/RailtownAI/railtracks).

## Examples

- **[Customer Support Triage Agent](customer-support)** — Ingest tickets with `rail-engine-ingest`, search with `rail-engine`, triage with a Railtracks agent (structured JSON output).
