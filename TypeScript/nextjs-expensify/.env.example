# For ingestion
ENGINE_TOKEN="[Your Engine Token]"

# For retrieval (list, delete, index search, vector search — VectorStore1)
ENGINE_ID="[Your Engine ID]"
ENGINE_PAT="[Your Engine PAT]"
# Optional: must match the stack your ENGINE_TOKEN targets (defaults to production API)
# RAILTOWN_API_URL="https://your-api-host/api"

# For receipt OCR via Claude vision
ANTHROPIC_API_KEY="[Your Anthropic API key]"
